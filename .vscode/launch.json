{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "lldb",
      "request": "launch",
      "name": "Debug",
      "program": "${workspaceFolder}/build/hello.exe",
      "args": [],
      "cwd": "${workspaceFolder}",
      "preLaunchTask": "CMake: clean and rebuild"
    }
  ]
}
